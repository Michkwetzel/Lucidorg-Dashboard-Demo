enum Permission { exec, employee, guest, error }

enum Product { org, hr, board }

enum NavBarButtonType { createAssessment_org, currentAssessment_org, newAssessment_org, companyInfo_org, logOut_org, home_org, results_org, impact_rg, theFix_org, closeMenu_org, export_org, howTo_org, home_hr, results_hr , newJobSearch_hr, home_board}